/**
 * @jest-environment node
 */

// const { testDb } = require('../../pgAdaptor');

const { updateBillStatus, updateSummaryUrls } = require('../update');

describe('updateBillStatus', () => {
  test('updateBillStatus should ...', () => {});
});

describe('updateSummaryUrls', () => {
  test('updateSummaryUrls should ...', () => {});
});
